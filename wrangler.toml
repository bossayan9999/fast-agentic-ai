# Cloudflare Pages project name
name = "fast-agentic-ai"
compatibility_date = "2024-12-01"
compatibility_flags = ["nodejs_compat"]

# Output directory produced by @cloudflare/next-on-pages
pages_build_output_dir = ".vercel/output/static"

# Optional: set public vars here (secrets go in dashboard or `wrangler pages secret`)
[vars]
# NEXT_PUBLIC_APP_URL = "https://fast-agentic-ai.pages.dev"
